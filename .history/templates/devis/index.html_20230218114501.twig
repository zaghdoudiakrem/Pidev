{% extends 'base2.html.twig' %}

{% block title %}list Devis{% endblock %}
{% block Sidebar %} 
<!-- ======= Sidebar ======= -->
		
			<aside id="sidebar" class="sidebar">

				<ul class="sidebar-nav" id="sidebar-nav">


					<!-- End Forms Nav -->
					<li class="nav-item">
						<a class="nav-link " href="{{ path('app_devis_index')}}">
							<i class="bi bi-person-workspace"></i>
							<span>Devis</span>
						</a>
					</li>

					<!-- End Tables Nav -->

					<li class="nav-item">
						<a class="nav-link " href="{{ path('app_devis_index')}}">
							<i class="bi bi-calendar2-date"></i>
							<span>Locale</span>
						</a>
					</li>
					<!-- End Icons Nav -->
				
					<li class="nav-heading">
						Pages</li>

					<li class="nav-item">
						<a class="nav-link collapsed" href="users-profile.html">
							<i class="bi bi-person"></i>
							<span>Profile</span>
						</a>
					</li>
					<!-- End Profile Page Nav -->


					<li class="nav-item">
						<a class="nav-link collapsed" href="pages-login.html">
							<i class="bi bi-box-arrow-in-right"></i>
							<span>Logout</span>
						</a>
					</li>
					<!-- End Login Page Nav -->


				</ul>

			</aside>
			<!-- End Sidebar-->
            {% endblock %}

{% block body %}
 <div class="container">
    <h1>List Devis </h1>
    <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-primary" href="{{ path('app_devis_new') }} "> 
                    créer un nouveau devis
                </a>
            </div>
<div class="card-body">
    <table class="table table-bordered">
        <thead>
            <tr>
             <th>Description</th>
                <th>Quantity</th>
                <th>Unit Price</th>
                <th>Total Price</th>
                <th>show</th>
                <th>Edit</th>
            </tr>
        </thead>
        <tbody>
        {% for devi in devis %}
            <tr>
                <td>{{ devi.id }}</td>
                <td>{{ devi.montant }}</td>
                <td>{{ devi.description }}</td>
                <td>{{ devi.etat ? 'Yes' : 'No' }}</td>
                <td>{{ devi.idExpert }}</td>
                <td>
                    <a  class="btn btn-outline-info" href="{{ path('app_devis_show', {'id': devi.id}) }}">show</a>
                </td>
                <td>
                <a  class="btn btn-outline-success" href="{{ path('app_devis_edit', {'id': devi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
    
    </div>
{% endblock %}
