{% extends 'base2.html.twig' %}

{% block title %}Devis{% endblock %}
{% block Sidebar %} 
<!-- ======= Sidebar ======= -->
		
			<aside id="sidebar" class="sidebar">

				<ul class="sidebar-nav" id="sidebar-nav">


					<!-- End Forms Nav -->
					<li class="nav-item">
						<a class="nav-link " href="{{ path('app_devis_index')}}">
							<i class="bi bi-clipboard-data"></i>
							<span>Devis</span>
						</a>
					</li>

					<!-- End Tables Nav -->

					<li class="nav-item">
						<a class="nav-link " href="{{ path('app_devis_index')}}">
							<i class="bi bi-house-gear
                            
                            q"></i>
							<span>Locale</span>
						</a>
					</li>
					<!-- End Icons Nav -->
                    <li class="nav-item">
						<a class="nav-link " href="{{ path('app_devis_index')}}">
							<i class="bi bi-calendar2-date"></i>
							<span>Rendez_Vous</span>
						</a>
					</li>
					<!-- End Icons Nav -->
				
					<li class="nav-heading">
						Pages</li>

					<li class="nav-item">
						<a class="nav-link collapsed" href="users-profile.html">
							<i class="bi bi-person"></i>
							<span>Profile</span>
						</a>
					</li>
					<!-- End Profile Page Nav -->


					<li class="nav-item">
						<a class="nav-link collapsed" href="pages-login.html">
							<i class="bi bi-box-arrow-in-right"></i>
							<span>Logout</span>
						</a>
					</li>
					<!-- End Login Page Nav -->


				</ul>

			</aside>
			<!-- End Sidebar-->
{% endblock %}

{% block body %}
 <div class="container">
 <br>
 <br>
 <br>
     <h1>Items for Devis {{ devi.id }}</h1>
    <div class="card">
            <div class="card-header">
                <a href="{{ path('app_devis_index') }}" class="btn btn-outline-info float-right" > 
                   Liste des devis
                </a>
            </div>

    <table class="table">
      <thead>
            <tr>
                <th>Description</th>
                <th>Quantity</th>
                <th>Unit Price</th>
                <th>Total Price</th>
            </tr>
    </thead>
        <tbody>
            {% for item in devi.items %}
                <tr>
                    <td>{{ item.description }}</td>
                    <td>{{ item.quantite }}</td>
                    <td>{{ item.unitPrice }}</td>
                    <td>{{ item.totalPrice }}</td>
                </tr>
            {% endfor %}
			        <tr>
                        <td colspan="3" class="text-right font-weight-bold">Total HT:</td>
                        <td>{{ devi.totalHT }}</td>
                    </tr>
        </tbody>
    </table>

<div class="card-body">
  
	<a  class="btn btn-outline-success" href="{{ path('app_devis_edit', {'id': devi.id}) }}">edit</a>
	<a href="{{ path('app_devis_generate_pdf', {'id': devi.id}) }}" class="btn btn-outline-danger float-right mr-2" target="_blank">Print</a>
	<a href="{{ path('app_send_email', {'id': devi.id}) }}" class="btn btn-outline-primary float-right mr-2">Send Email</a>

       $entityManager = $this->getDoctrine()->getManager();
$expert = $entityManager->getRepository(User::class)->find($devi->getIdExpert());

// Get the expert's email
$expertEmail = $expert->getEmail();     
</div>
   
    </div>
{% endblock %}
