{% extends 'base2.html.twig' %}

{% block title %}Nouvelle Reclamation{% endblock %}
 
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Créer une nouvelle réclamation</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_reclamation_index') }}"> 
                   Retour à la liste
                </a>
            </div>
            <div class="card-body">
                 {{ include('reclamation/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}



