{% extends 'base2.html.twig' %}

{% block title %}Edit Vehicule{% endblock %}

{% block body %}
    <div class="container">
      <h2 class="text-center mt-5 mb-3">Modifier Vehicule</h2>
     
      <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_constat_index') }}"> Retour </a>

    <table class="table table-bordered">
    <tr>
    <td>{{ include('vehicule/_form.html.twig') }}</td>
    </tr>
    </table>

{% endblock %}
