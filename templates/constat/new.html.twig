{% extends 'base.html.twig' %}

{% block title %}Nouvelle Constat{% endblock %}

{% block body %}
<div class="container">
        <h2 class="text-center mt-5 mb-3">Ajouter un Constat</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_constat_index') }}"> 
                   Retour 
                </a>
            </div>
            <div class="card-body">
                 {{ include('constat/_form.html.twig') }}
            </div>
        </div>
    </div>
    {% endblock %}